{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Launch Program",
      "program": "${workspaceFolder}/index.js", // Replace 'index.js' with your entry file
      "cwd": "${workspaceFolder}",
      "runtimeVersion":"16.15.1",
      "console": "integratedTerminal", // Use 'integratedTerminal' or 'internalConsole'
    }
  ]
}
